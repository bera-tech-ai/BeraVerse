{
  "name": "beraverse",
  "version": "1.0.0",
  "description": "An open YouTube-like video platform",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no tests specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
